(function () {
  'use strict';

  angular
    .module('paymewDistributor')
    .controller('RuleController', RuleController);

  /** @ngInject */
  function RuleController($timeout, webDevTec, toastr) {
    var vm = this;
  }
})();
